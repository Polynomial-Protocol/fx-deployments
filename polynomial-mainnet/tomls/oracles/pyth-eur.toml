# This registers a staleness node that looks to Pyth for a fresh enough price, or otherwise reverts with an ERC-7412 compatible error
# Latest Pyth Price
[invoke.registerPythEurOracleNode]
target = ["system.oracle_manager.Proxy"]
func = "registerNode"
args = [
    5, # 5 = pyth aggregator type
    "<%= defaultAbiCoder.encode(['address', 'bytes32', 'bool'], [settings.pyth_price_verification_address, settings.pythEurFeedId, false]) %>",
    [
    ],
]
depends = ["provision.system"]
extra.eur_pyth_oracle_id.event = "NodeRegistered"
extra.eur_pyth_oracle_id.arg = 0

# Pyth Off-chain Lookup
[invoke.registerLookupEurOracleNode]
target = ["system.oracle_manager.Proxy"]
func = "registerNode"
args = [
    9, # 9 = PythOffchainLookupNode
    "<%= defaultAbiCoder.encode(['address', 'bytes32', 'uint256'], [imports.pyth_erc7412_wrapper.contracts.PythERC7412Wrapper.address, settings.pythEurFeedId, settings.bigCapDefaultStalenessTolerance]) %>",
    [
    ],
]
depends = ["provision.system"]
extra.eur_lookup_oracle_id.event = "NodeRegistered"
extra.eur_lookup_oracle_id.arg = 0

# Staleness Node
[invoke.registerEurOracleNode]
target = ["system.oracle_manager.Proxy"]
func = "registerNode"
args = [
    7, # 7 = staleness circuit breaker
    "<%= defaultAbiCoder.encode(['uint256'], [settings.bigCapDefaultStalenessTolerance]) %>",
    [
        "<%= extras.eur_pyth_oracle_id %>",
        "<%= extras.eur_lookup_oracle_id %>",
    ],
]
depends = ["provision.system"]
extra.eur_oracle_id.event = "NodeRegistered"
extra.eur_oracle_id.arg = 0
